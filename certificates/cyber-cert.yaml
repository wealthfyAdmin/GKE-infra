apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cyberambassador
  namespace: gateway-system
spec:
  secretName: cyberambassador-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - api.cyberambassador.co.in
